---
// CTA Section - Final call to action
---

<section class="py-20 bg-gradient-to-br from-red-600 via-pink-600 to-purple-600">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Urgency Badge -->
    <div class="inline-flex items-center gap-2 bg-yellow-400 text-black px-6 py-3 rounded-full font-bold text-lg mb-8">
      <span>🔥</span>
      <span>Oferta Limitada</span>
    </div>

    <!-- Main CTA -->
    <h2 class="text-4xl sm:text-5xl font-bold text-white mb-6">
      Sua Última Chance de
      <br>
      <span class="text-yellow-300">Transformar Sua Vida</span>
    </h2>

    <p class="text-xl text-pink-100 mb-8 max-w-3xl mx-auto">
      Milhares já estão faturando com vídeos automáticos. Não deixe essa oportunidade passar e continue vendo os outros prosperarem enquanto você fica para trás.
    </p>

    <!-- Price Display -->
    <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-8 border border-white/20 mb-8">
      <div class="text-center">
        <div class="text-white/70 text-lg mb-2">De R$ 2.997 por apenas</div>
        <div class="text-6xl font-bold text-white mb-4">R$ 197</div>
        <div class="text-pink-200 mb-6">Pagamento único • Acesso vitalício • Sem mensalidades</div>
        
        <!-- Countdown Timer -->
        <div class="bg-black/20 rounded-lg p-4 mb-6">
          <div class="text-yellow-300 font-semibold mb-2">Oferta expira em:</div>
          <div class="flex justify-center gap-4 text-white" id="countdown">
            <div class="text-center">
              <div class="text-2xl font-bold" id="days">00</div>
              <div class="text-sm">DIAS</div>
            </div>
            <div class="text-center">
              <div class="text-2xl font-bold" id="hours">00</div>
              <div class="text-sm">HORAS</div>
            </div>
            <div class="text-center">
              <div class="text-2xl font-bold" id="minutes">00</div>
              <div class="text-sm">MIN</div>
            </div>
            <div class="text-center">
              <div class="text-2xl font-bold" id="seconds">00</div>
              <div class="text-sm">SEG</div>
            </div>
          </div>
        </div>

        <!-- Main CTA Button -->
        <button class="bg-gradient-to-r from-yellow-400 to-orange-400 hover:from-yellow-500 hover:to-orange-500 text-black font-bold text-xl py-6 px-12 rounded-2xl transform hover:scale-105 transition-all duration-300 shadow-2xl mb-4">
          🚀 GARANTIR AGORA
        </button>
        
        <div class="text-pink-200 text-sm">
          ✓ Instalação imediata • ✓ Suporte completo • ✓ Garantia 7 dias
        </div>
      </div>
    </div>

    <!-- Risk Reversal -->
    <div class="bg-green-500/20 backdrop-blur-sm rounded-xl p-6 border border-green-400/30 mb-8">
      <div class="flex items-center justify-center gap-3 mb-3">
        <div class="text-3xl">🛡️</div>
        <h3 class="text-xl font-bold text-white">Garantia Blindada - Zero Risco</h3>
      </div>
      <p class="text-green-100">
        Teste por 7 dias completos. Se não ficar satisfeito, devolvemos 100% do seu dinheiro sem perguntas. Você não tem nada a perder, apenas a ganhar.
      </p>
    </div>

    <!-- Social Proof -->
    <div class="flex flex-wrap justify-center items-center gap-8 mb-8 text-white/80">
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
          <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <span>2.847+ clientes ativos</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
        </svg>
        <span>4.9/5 satisfação</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
        </svg>
        <span>R$ 3.2M+ gerados</span>
      </div>
    </div>

    <!-- Final Message -->
    <div class="max-w-2xl mx-auto">
      <p class="text-lg text-pink-100 mb-6">
        <strong>Você tem duas escolhas:</strong> Continuar como está, vendo outros prosperarem com IA, ou dar o primeiro passo rumo à sua independência financeira hoje mesmo.
      </p>
      
      <p class="text-pink-200">
        O mercado de vídeos IA está explodindo. Cada dia que passa, mais pessoas descobrem essas estratégias. Não deixe o trem passar sem você dentro.
      </p>
    </div>

    <!-- Secondary CTA -->
    <div class="mt-12">
      <button class="bg-white/20 hover:bg-white/30 border-2 border-white text-white font-semibold py-4 px-8 rounded-lg transition-all">
        Ver Demonstração Completa (12:47)
      </button>
    </div>
  </div>
</section>

<script>
  // Countdown Timer
  function updateCountdown() {
    const now = new Date().getTime();
    const targetDate = now + (3 * 24 * 60 * 60 * 1000); // 3 days from now
    const distance = targetDate - now;

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById('days').textContent = days.toString().padStart(2, '0');
    document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
    document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
    document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');

    if (distance < 0) {
      document.getElementById('countdown').innerHTML = '<div class="text-yellow-300 font-bold">OFERTA EXPIRADA!</div>';
    }
  }

  // Update countdown every second
  setInterval(updateCountdown, 1000);
  updateCountdown(); // Initial call
</script>
